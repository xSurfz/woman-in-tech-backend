version: "3.9"

services:
  backend:
    build: .
    container_name: woman-in-tech-backend

    ports:
      - "3000:3000"

    env_file:
      - .env

    volumes:
      - ./prisma:/app/prisma
      - ./uploads:/app/uploads

    restart: unless-stopped